
@if (Session["AdminID"] == null)
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("你好，请登录！", "Login", "Admin", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>
            @Html.ActionLink("你好，" + Session["Accounts"].ToString() + " !", "Index", "Admin", routeValues: null, htmlAttributes: new { title = "点击进入管理后台" })
        </li>
        <li>
            @Html.ActionLink("点击退出", "Logout", "Admin")
        </li>
    </ul>
}
